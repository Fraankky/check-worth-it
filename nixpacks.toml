[phases.setup]
nixPkgs = ["python311", "playwright-driver"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "cd apps/scraper-api && pip install -r requirements.txt",
  "playwright install chromium"
]

[start]
cmd = "cd apps/scraper-api && uvicorn main:app --host 0.0.0.0 --port $PORT"